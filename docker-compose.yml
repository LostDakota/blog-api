version: '3'

services:
    nodejs:
        image: node
        container_name: node
        working_dir: /usr/app
        restart: unless-stopped
        ports:
            - "3001:3001"
        network_mode: "host"
        volumes: 
            - ./src:/usr/app
            - /var/www/api/config:/usr/app/config
        command: bash -c 'npm install && node index.js'
